#!/usr/bin/env bash

TIME=`date +%s`
python census.py > ../data/historical/$TIME.json

cd ../data/
git pull
cp historical/$TIME.json latest.json
git add .
git commit -m "Auto-add latest data"
git push origin gh-pages
